import React from "react";

const CustomTitle = ({ title }) => {
  return (
    <h2 className="text-2xl capitalize text-center font-bold text-blue-700 ">
      {title}
    </h2>
  );
};

export default CustomTitle;
